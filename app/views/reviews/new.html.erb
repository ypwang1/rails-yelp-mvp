<h1>Create a new review</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: [1, 2, 3, 4, 5], prompt: "Select a rating"%>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
